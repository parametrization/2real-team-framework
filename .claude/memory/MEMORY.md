# Project Memory — 2real-team-framework

One line per memory; topic files in `.claude/memory/*.md` are read on demand. Recording a
memory: create/edit `<kebab-slug>.md` with frontmatter (`name`, `description`, `metadata.type`),
add a pointer line here, link related memories with `[[slug]]`, and commit it so it travels with
the branch. Update an existing file rather than duplicating; delete memories that turn out wrong.

- [Framework extraction state](project_framework_extraction_state.md) — baseline: **Wave 23 (Phase 7 W2) "The record must not lie" ROLLED UP to main (PR #316, merge `cdbc009`, 12/12 CI green) — NO RELEASE CUT (owner stopped before tag/PyPI/npm). 4 file-disjoint stories, all 2/2 gate-approved: S1 #296/PR #306 `_NEGATIVE_SIGNAL_FIELDS` + `_VACUOUS_BODY_RE` rejects `"Name: None"`/`-`/`n/a`/blank [Paloma; 1 CR — Tariq caught #296's own failure mode surviving inside #296's fix: `_BARE_NONE_RE` was whole-line-anchored but the emitter only ever produces `"{name}: {gap}"`]; S2 #300/PR #307 `/wave-end` derives counters from `trust_signals extract` [Nia; **2 CR** — Paloma caught `.author.login` grouping (one GH account for whole team → 100% concentration every wave vs true 25%), then caught the abort guard anchoring on `gh pr list`, which shares the GraphQL bucket `extract` drains, so under pre-exhausted budget both die and the guard certifies `0==0`; its `test_genuinely_empty_wave_proceeds` had ENCODED the bug as a requirement. Now anchors on `gh api rate_limit` = REST, orthogonal bucket]; S3 #301/PR #305 reserved-5 pluralized to "top relative performer(s)" [Ibrahim, clean — wave's only clean-first-pass]; S4 #304/PR #309 `validate_pr_body.py` hook [Tariq; 1 CR — Nia caught 4 closing keywords in his own commit MESSAGE, 2 bound to #296, which would have auto-closed #296 from the wrong commit; his hook masks backticks, right for PR bodies (markdown), WRONG for commit messages (plain text) → #312]. All 4 issues closed from **commit trailers**, each attributed to its own author (#296 ← Paloma's `6bd8c7a`) — the point of #304. Counters `4/3/25%`, review load even (2v/2prs each), **zero drift, first since W12** (derived with S2's own code). 🚨 **TRUST SCORING PARKED — #314: `composite()` omits `missed_catches` from its negative set while `score_delta()` charges −1 each → Ibrahim holds the HIGHEST composite (7) AND the WORST delta (−2→1), and since reserved-5 needs `composite==top` his 7 caps Paloma+Tariq 5→4 for a THIRD engineer's number. Root cause: `verified_reviews=2` and `missed_catches=2` come from the SAME two reviews — `_has_verified_checks` validates a receipt is detailed, not that the review worked.** Owner parked all deltas (Paloma 5, Tariq 5, Nia 3, Ibrahim 3 held); mechanical output recorded as evidence. 2nd consecutive wave Ibrahim's positives gated away. 6 issues filed: #308 (charter still claims merge gate doesn't author-exclude — false since #293/#294; **owner scoped into W24**), #310, #311 (`extract` → `{}` at exit 0 under rate limit), #312, #313 (`review_load.py` breaks in framework's own tree while its comment claims otherwise — W22's review-load figure is suspect), #314. Next stub #315 (theme TBD; `global_wave_seq` still 23). ⚠️ **`gh` caches HTTP in `/tmp/gh-cli-cache` NOT `~/.cache/gh`; `gh pr merge` sends `X-Gh-Cache-Ttl: 24h` so a GraphQL rate-limit error caches as a 200 and REPLAYS for 24h** (frozen `X-Ratelimit-Reset`, identical `X-Github-Request-Id`) — cost 80 min. ⚠️ **Never pipe `gh pr merge` through `sed`/`grep` under `set -e`** — the filter's exit code wins; reported 4 merges that never happened. Verify with `gh api repos/O/R/pulls/N --jq .merged`. ⚠️ `pr_review_state.py` blocker key is `unresolved_must_fix`, not `.blockers`. ⚠️ merge gate matches only `gh pr (ready|merge)` — `gh api graphql`/REST/local push all bypass it. PRIOR W16 baseline below]. Read first.
  <!-- W16 prior --> baseline: released v0.11.1 (Phase 6 W16 "Reward the reviewer + clear the gate debt" — 2 file-disjoint stories: S1 #275/PR #286 reserved-5 composite fix [credit `must_fix_caught`+`verified_reviews` at `REVIEW_VALUE_WEIGHT=2`, cap `difficulty_points` at `DIFFICULTY_COMPOSITE_CAP=2`; scale kept 1–5] [Paloma→Nia+Tariq, clean]; S2 #284/PR #287 docs/refactor `load_bearing_test_exceptions` class [default-on `docs`, dual-tree, `_deep_merge` so downstream can't zero by omission] [Ibrahim→Paloma+Tariq; **1 CR cycle** — BOTH reviewers independently reproduced a `_patch_is_docs_only` self-close+trailing-code bypass (`"""x""";import os` misread as unterminated multi-line open → swallowed real lines past gate) w/ executing PoCs; Ibrahim fixed fail-safe symmetric across opening-line+interior-line branches, 5 revert→red tests]; 2 PRs/1 CR/50% conc; **✅ TRUST UN-PARKED — #275 CLOSED: first wave under new composite + first deltas applied since W18 → Paloma 4→5 [reserved-5 rotated IN; composite 5, out-contributed on BOTH author+reviewer axes — caught the #287 bypass AND shipped hardest clean PR], Tariq 5→4 [rotated OFF single-seat; his pure-reviewer composite rose ~0→4 under the fix = the fix WORKING, NOT the W20 clean-author-displaces-catcher failure mode], Nia 4→4, Ibrahim 4→3 [`must_fix_received=2`+`rework=1`]; scale-broaden trigger never fired**; npm publish hit CI drift [npm@12 needs node≥22, runner node 20 → EBADENGINE] hotfixed by pin `npm@^11.5.1`+`workflow_dispatch`, both registries at 0.11.1; new debt #288 [make `trust_signals`+`review_load` author-exclusive like merge gate — W21 author-note verdict-grammar slip counted author as 3rd reviewer, spurious `missed_catches`/`verified_reviews`, worked around by amend]; `gh api` PATCH bodies MUST use `-F body=@file` NOT `-f` [literal-string trap, hit twice]; next stub #289 wave 22 [Phase 7 opener — sibling-repo mining #264/#102]; PRIOR W15 baseline below]. Read first.
  <!-- W15 prior --> v0.11.0 (Phase 6 W15 "Restore-story closeout" — 2 file-disjoint stories: S1 #279/PR #283 git-native `2real-team install-branch` staged install [engine `framework/install/install_branch.py` + bridge `stage_install_framework`; throwaway branch off HEAD, one commit, `git merge` to keep / `git branch -D` to discard; refuses non-git/dirty; composes w/ manifest restore] [Ibrahim→Paloma+Tariq; **1 CR cycle** — Tariq caught packaged cmd passed `install_config=None`→`repo.expect=fresh`→refused every realistic repo; fixed by `--expect=any` default + exposing `--expect`/`--config` + end-to-end success test]; S2 #280/PR #282 node CLI teardown/restore parity [`uninstall`+`restore` bridged to bundled Python; `buildTeardownArgv`/`runTeardown`+aliases+`describeTeardownDegradation`; `--dry-run`/`--non-interactive` passthrough] [Nia→Ibrahim+Tariq, clean]; 2 PRs/1 CR/50% conc; restore-family (#278→#279→#280) now complete both surfaces; ⚠️ **trust scoring STILL PARKED — #275 data-point #2: distribution HEALTHY [`[5,5,3,4]` spread 2 var 0.69 NOT degenerate] so scale-broadening trigger did NOT fire; BUT reserved-5-composite trigger FIRED — mechanical result rotates reserved-5 Tariq[caught wave's only defect]→Nia[clean author] via author-only `difficulty_points`; matrix HELD Tariq 5/others 4; recommend crediting `must_fix_caught`+`verified_reviews` in composite, NOT range widen [issuecomment-4919046932]**; new debt #284 [docs/refactor load-bearing exception, W20 docstring-only edit tripped gate]; deferred to Phase 7: #264/#102 sibling-repo mining; next stub #285 wave 21; PRIOR W13 baseline below]. Read first.
  <!-- W13 prior --> v0.10.5 (Phase 6 W13 "Harden the machinery"): S1 #258/#259/PR #266 `verified_reviews` per-(reviewer,PR) dedup + tightened `_VERIFIED_CHECK_RE` [Paloma]; S2 #262/PR #267 rulesets-aware CI gate [`_rulesets_enforce_required_checks`; hook wired IN PLACE, no `.claude/hooks/` mirror] [Nia]; S3 #251/#252/PR #268 `require_children` `--real-config` surface + node unparseable-card warn [Ibrahim]; 3 PRs/0 CR/33%; score 18: Tariq +1→5 EARNED, others 4; ⚠️ #270 arrow false-negative FIXED in W19/#277.
  <!-- W12 prior --> Phase 6 W12 "Symmetric trust scoring + rollup hygiene" v0.10.4 — 2 file-disjoint stories: S1 #254/PR #257 made scoring SYMMETRIC [owner chose option-3 "verified-clean-review" but required downward counter-balance]: new `verified_reviews` +1 on clean wave at ≥2 [substantive `Verified:` block w/ concrete checks; anti-gaming `_has_verified_checks` rejects empty/boilerplate] + newly-active `rework_cycles≥2` −1 + `must_fix_received` ding tightened ≥3→≥2, 9 revert→red tests, Paloma→Nia+Tariq; S2 #255/PR #256 rollup-hygiene charter runbook step [fetch + merge `origin/<wave>` explicitly, never stale LOCAL ref, verify feature-code on merge parent] codifying the W11 slip, dual-tree byte-parity + template-render, Ibrahim→Tariq+Paloma; 2 PRs/0 CR/50% conc; all 4 verdicts clean first-pass; **score 17 all delta 0** — `verified_reviews` debut validated BOTH directions [credited 2 substantive #257 blocks Nia+Tariq, REJECTED 2 empty #256 blocks Tariq+Paloma], each reviewer at 1<2 threshold → no bump; no decay/no retirement; **Tariq 5→5 reserved-HELD [now threshold-bound: rotation waits on a wave large/concentrated enough to clear ≥2 verified reviews], Nia/Paloma/Ibrahim 4→4**; both stories DOGFOODED this wave [symmetric signal scored this retro; rollup step used in this wave's own rollup → content-probe PASS]; new debt #258 per-(reviewer,PR) dedup / #259 tighten bare-`determinism` regex), OIDC both registries; next stub #260 (wave 18, theme TBD); deferred: rulesets branch-protection probe, #251/#252, more #102 P2, #110. Read first.
- [Config-driven architecture](reference_config_driven_architecture.md) — config→assets→bootstrap→dispatcher; one shared-config object, stdlib-only fail-open, hook contract, `cfg.path.parent.parent` = repo root.
- [Lifecycle state machine](reference_lifecycle_state_machine.md) — lifecycle.py: monotonic wave allocator, merge models, classify_reachability, transitions + CLI.
- [Per-child union rosters](reference_per_child_union_rosters.md) — roster_gen partition (meta=org roles, child=lead+domain engineers); identity gate enforces meta∪child via parent-merge.
- [CLI framework bundling](reference_cli_framework_bundling.md) — hatch BundleSharedDataHook → `real_team/_bundled/framework`; `init --with-hooks` subprocesses the bundled bootstrap `--no-team`.
- [upsert_status_keys seeding](project_upsert_status_keys_seeding.md) — can't seed empty `{}`; first write must be seeded directly in compact multi-line shape (lifecycle `_initial_text`).
- [Commit/PR mechanics](feedback_framework_commit_pr_mechanics.md) — owner identity via `-c`, msgs via `-F file`, title via `gh api PATCH` (projects-classic), push unpiped; CI runs `ruff check` only.
- [Session handoff](handoff.md) — latest pickup point; read first at session start.

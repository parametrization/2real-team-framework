# Wave-kickoff orchestration scripts

This directory holds per-wave orchestration scripts that supplement
`/wave-kickoff` (project-board adds, label sweeps, branch-creation
helpers). Tracked for audit-trail visibility — see #247.

File naming: `w{N}-{purpose}.py` (e.g., `w4-kickoff.py`).
